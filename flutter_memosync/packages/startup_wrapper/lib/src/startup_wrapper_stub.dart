class LaunchStartup {
  static Future<void> init() => Future(() {});
  static Future<void> enable() => Future(() {});
  static Future<void> disable() => Future(() {});
  static Future<bool> isEnabled() => Future(() => false);
}
